package com.example.letsbuy.service

import retrofit2.http.POST
import retrofit2.http.Query

interface ImageService {

    //@POST("/adversiment/9")
    //fun uploadImages(@Query("img1") img1: Multipart)
}